fastapi
uvicorn[standard]
SQLAlchemy
psycopg2-binary
python-dotenv
gunicorn
PyJWT
python-multipart

# Model serving (assess-risk). The saved model is a CalibratedClassifierCV over an
# imbalanced-learn pipeline, so imbalanced-learn MUST be installed to unpickle it.
# scikit-learn is pinned to the training version (its pickles are version-sensitive);
# numpy 2 / newer xgboost load the bundle fine via backward compatibility (verified).
scikit-learn==1.6.1
imbalanced-learn==0.14.2
xgboost
numpy
joblib
pandas
